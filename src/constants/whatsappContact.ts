/** Digits only — used in `https://wa.me/{id}` (same business line as the floating chat button). */
export const ONUKPA_WHATSAPP_WA_ME_ID = "233245095569";

/** Default prefill when a renter taps “Get started” / find a place. */
export const ONUKPA_WA_RENTER_DEFAULT =
  "Hi Onukpa! I need help finding a place to rent";

/** Prefill for agents starting onboarding on the bot (no web registration). */
export const ONUKPA_WA_AGENT_ONBOARD =
  "Hi Onukpa! I'm an agent and want to register and list properties.";
