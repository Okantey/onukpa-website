# Admin + auth (axios) — must include the /api prefix used by onukpa-bot/bot.ts
VITE_API_URL=http://localhost:3000/api

# Public marketing/supplier fetch helper (src/api/http.ts) — origin only, no path
# VITE_ONUKPA_API_BASE_URL=https://api.onukpa.com
